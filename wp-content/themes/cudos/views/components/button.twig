<a class="button {{ class }}" href="{{ button.url }}" target="{{ button.target ? button.target : '_self' }}"
{% if label %}
title="{{ label }}"
{% endif %}
> 
  <span class="button__bg"></span>
  <span class="button__text">{{ button.title }}</span>
</a>
